<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4caf50;
            --secondary-color: #f9f9f9;
            --gradient-color: linear-gradient(135deg, #e3ffe7, #d9e7ff);
        }
        body {
            background: var(--gradient-color);
            min-height: 100vh;
        }
        .order-success-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
        }
        .order-success-icon {
            font-size: 6rem;
            color: var(--primary-color);
        }
        .order-details {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 20px;
        }
        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            transition: transform 0.2s ease, background-color 0.3s ease;
        }
        .btn-primary:hover, .btn-primary:focus {
            background-color: #45a049;
            border-color: #45a049;
            transform: scale(1.05);
        }
        .btn-outline-primary {
            color: var(--primary-color);
            border-color: var(--primary-color);
            transition: transform 0.2s ease, background-color 0.3s ease;
        }
        .btn-outline-primary:hover, .btn-outline-primary:focus {
            background-color: var(--primary-color);
            color: #ffffff;
            transform: scale(1.05);
        }
        .text-highlight {
            color: var(--primary-color);
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card order-success-card">
                    <div class="card-body text-center p-5">
                        <i class="fas fa-check-circle order-success-icon mb-4"></i>
                        <h1 class="card-title mb-4 text-highlight">Order Successful!</h1>
                        <p class="lead mb-5">Thank you for your purchase. Your order has been processed successfully.</p>
                        
                        <div class="order-details">
                            <h5 class="mb-4 text-highlight">Order Details</h5>
                            <div class="row mb-3">
                                <div class="col-6 text-start">
                                    <i class="fas fa-calendar-alt me-2"></i>Order Date
                                </div>
                                <div class="col-6 text-end"><%= date %></div>
                            </div>
                            <div class="row">
                                <div class="col-6 text-start">
                                    <i class="fas fa-money-bill-wave me-2"></i>Total Amount
                                </div>
                                <div class="col-6 text-end fw-bold">₹<%= totalPrice %></div>
                            </div>
                        </div>
                        
                        <div class="d-grid gap-2 mt-4">
                            <a href="/userProfile" class="btn btn-primary btn-lg">View Order Details</a>
                            <a href="/shop" class="btn btn-outline-primary btn-lg">Continue Shopping</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
